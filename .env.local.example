# Published-CSV URL of your "Seasons" control tab (File → Share → Publish to web → CSV).
# This is the app's index of which seasons exist.
SEASONS_CSV_URL=

# Optional: published-CSV URL of your "History" tab (Season | Place | Team | Points)
# for the all-time standings on the History page.
HISTORY_CSV_URL=
